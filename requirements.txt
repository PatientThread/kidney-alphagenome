# Pinned to the versions used for the published analysis (13-14 September 2026).
# Python 3.12; the system Python 3.9 cannot install alphagenome.
alphagenome==0.9.0
pandas>=2.0
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
